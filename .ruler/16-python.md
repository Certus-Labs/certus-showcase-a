# Python Stack

We will be using UV + Ruff with a local .venv, which should be activated when prompts try to execute python code.

For unit testing, we will be using pytest and mocker (not the one in unittest), with conftest files for fixtures.
